# Historical deployment guard. The active Netlify site is configured from the repository root.
[build]
  command = "npm run historical:blocked"
  publish = "historical-deployment-disabled"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "npm run historical:blocked"
  targetPort = 3000
